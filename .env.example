# Onde o app do I.V.E responde.
#
# Em desenvolvimento é o Vite do ui/ (cd ui && npm run dev).
# Em produção, o domínio onde o produto estiver publicado.
#
# Copie para .env.local — o .env.local não vai pro git.
NEXT_PUBLIC_IVE_URL=http://localhost:5173
